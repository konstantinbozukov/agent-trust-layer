{
  "name": "agent-trust-layer-monorepo",
  "version": "0.1.1",
  "private": true,
  "description": "Open protocol for agent trust consult before spend — spec, reference core, compat tests",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/reference-express"
  ],
  "scripts": {
    "build": "npm run build -w agent-trust-layer && npm run build -w @agent-trust-layer/reference-express",
    "test": "npm run build && node --test tests/compat/*.test.js",
    "compat": "npm test",
    "example:with-atl": "npm run build && node examples/with-atl/index.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
